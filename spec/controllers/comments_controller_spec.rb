require 'rails_helper'

RSpec.describe CommentsController, type: :controller do

  # describe "POST #create", focus: true do
  #   let(:user) { FactoryGirl.create(:user) }
  #
  #   it "saves comments against a post" do
  #     post = Post.create(user_id: user.id, description: 'Best brunch place', image_file_name: 'image_file_name')
  #     user_2 = FactoryGirl.create(:user)
  #     sign_in user_2
  #     expect { post :create, :comment => { :content => "Looks great!", :post_id => post.id, :user_id => user2.id } }.to change { Comment.count }
  #   end
  # end
end
